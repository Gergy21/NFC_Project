<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">

    <!-- Largeur et couleur de la bordure -->
    <stroke
        android:width="4dp"
        android:color="@android:color/holo_red_dark"
        />

    <!-- rayon permettant d'avoir les coins du rectangle arrondi -->
    <corners
        android:radius="50dp"
        />

    <!-- Couleur du background-->
    <solid
        android:color="@android:color/background_dark" >
    </solid>

    <!-- Paramètres concernant la séparation entre la zone de texte et la bordure -->
    <padding
        android:bottom="2dp"
        android:left="5dp"
        android:right="5dp"
        android:top="2dp"
        />

</shape>